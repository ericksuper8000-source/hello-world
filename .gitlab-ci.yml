image: python:3.11
# GitLab crea una computadora temporal con Python 3.11

run:
  # Nombre del job
  script:
    # Comandos que ejecuta GitLab
    - python git_practica.py
    # Ejecuta el archivo git_practica.py, Python lee el archivo de arriba a abajo, Si todo va bien pipeline verde, si no pipeline rojo